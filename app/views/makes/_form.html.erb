<%= form_with(model: make, local: true) do |form| %>
  <% if make.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(make.errors.count, "error") %> prohibited this make from being saved:</h2>

      <ul>
      <% make.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :make_user_id %>
  </div>

  <div class="field">
    <%= form.label :categoe %>
    <%= form.text_field :categoe, id: :make_categoe %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
